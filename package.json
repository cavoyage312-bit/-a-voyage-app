{
  "name": "ca-voyage",
  "version": "1.0.0",
  "private": true,
  "description": "Plateforme de réservation multimodale - Vols, Hôtels, Bus, Voitures",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": "20.x"
  }
}